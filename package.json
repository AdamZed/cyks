{
  "name": "cyks",
  "devDependencies": {
    "prettier": "^3.7.4"
  },
  "description": "Can you keep score? A simple scoreboard webapp to track of 1v1 match",
  "scripts": {
    "format": "prettier --write .",
    "check": "prettier --check ."
  }
}
